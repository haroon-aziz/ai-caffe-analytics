# models/

Stores model weight files only (e.g. `yolo26n.pt`) — not Python code.
Weights are downloaded automatically on first run (see `config.py`,
added in Phase 2) or can be placed here manually. Weight files are
git-ignored; only this README is tracked so the directory exists in
a fresh checkout.
